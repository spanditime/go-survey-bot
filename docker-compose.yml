

services:
  go-survey-bot:
    build: 
      context: ./
      dockerfile: Dockerfile

    environment:
      - SURV_DATE_SAVE_LOCATION
      - GOOGLE_CREDENTIALS_JSON
      - GOOGLE_SPREADSHEET_ID
      - GOOGLE_SHEET_NAME
      - VK_BOT_TOKEN
      - TELEGRAM_BOT_TOKEN
